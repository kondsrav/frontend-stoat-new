import {
  $
} from "./chunk-Z4BPJRT6.js";
import {
  isDocument,
  isFunction,
  isString,
  isWindow
} from "./chunk-ZHXSUVOL.js";

// ../../node_modules/.pnpm/@mdui+jq@3.0.2/node_modules/@mdui/jq/methods/is.js
$.fn.is = function(selector) {
  let isMatched = false;
  if (isFunction(selector)) {
    this.each((index, element) => {
      if (selector.call(element, index, element)) {
        isMatched = true;
      }
    });
    return isMatched;
  }
  if (isString(selector)) {
    this.each((_, element) => {
      if (isDocument(element) || isWindow(element)) {
        return;
      }
      if (element.matches.call(element, selector)) {
        isMatched = true;
      }
    });
    return isMatched;
  }
  const $compareWith = $(selector);
  this.each((_, element) => {
    $compareWith.each((_2, compare) => {
      if (element === compare) {
        isMatched = true;
      }
    });
  });
  return isMatched;
};
//# sourceMappingURL=chunk-ECER6HRX.js.map
