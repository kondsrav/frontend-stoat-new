import {
  $,
  JQ,
  unique
} from "./chunk-Z4BPJRT6.js";
import {
  eachArray,
  isElement
} from "./chunk-ZHXSUVOL.js";

// ../../node_modules/.pnpm/@mdui+jq@3.0.2/node_modules/@mdui/jq/methods/slice.js
$.fn.slice = function(...args) {
  return new JQ([].slice.apply(this, args));
};

// ../../node_modules/.pnpm/@mdui+jq@3.0.2/node_modules/@mdui/jq/methods/eq.js
$.fn.eq = function(index) {
  const ret = index === -1 ? this.slice(index) : this.slice(index, +index + 1);
  return new JQ(ret);
};

// ../../node_modules/.pnpm/@mdui+jq@3.0.2/node_modules/@mdui/jq/methods/utils/dir.js
var dir = ($elements, nameIndex, node, selector, filter) => {
  const ret = [];
  let target;
  $elements.each((_, element) => {
    target = element[node];
    while (target && isElement(target)) {
      if (nameIndex === 2) {
        if (selector && $(target).is(selector)) {
          break;
        }
        if (!filter || $(target).is(filter)) {
          ret.push(target);
        }
      } else if (nameIndex === 0) {
        if (!selector || $(target).is(selector)) {
          ret.push(target);
        }
        break;
      } else {
        if (!selector || $(target).is(selector)) {
          ret.push(target);
        }
      }
      target = target[node];
    }
  });
  return new JQ(unique(ret));
};

// ../../node_modules/.pnpm/@mdui+jq@3.0.2/node_modules/@mdui/jq/methods/parent.js
eachArray(["", "s", "sUntil"], (name, nameIndex) => {
  $.fn[`parent${name}`] = function(selector, filter) {
    const $nodes = !nameIndex ? this : $(this.get().reverse());
    return dir($nodes, nameIndex, "parentNode", selector, filter);
  };
});
//# sourceMappingURL=chunk-5IG7YWYZ.js.map
